# Sessão de 2026-09-03

## Regra de rastreabilidade de tarefas

Status: **concluído e validado**.

- Foi estabelecida em `CLAUDE.md` a regra permanente de registrar cada tarefa,
  etapa ou PR assim que for concluída, antes de comunicar a conclusão;
  `AGENTS.md` expõe a mesma obrigação aos demais agentes do projeto.
- O registro distingue `não iniciado`, `em andamento`, `implementado, não
  validado`, `concluído e validado` e `bloqueado`.
- Commit, push e deploy passaram a ser estados independentes, para impedir que
  “código pronto” seja confundido com “já está no ar”.
- O comando canônico `.claude/commands/encerrar-sessao.md` agora reconcilia o
  Git, este histórico e o roadmap, servindo como verificação final contra
  tarefas esquecidas.
- As duas instruções antigas e divergentes dentro do diretório do backend
  viraram apontadores de compatibilidade. A única fonte do procedimento de
  encerramento passa a ser o comando na raiz do monorepo.

Validação executada: revisão do diff documental e `git diff --check` nos
arquivos alterados por esta tarefa. Não foram executadas suítes de código,
pois a mudança é exclusivamente de processo/documentação.

Estado de entrega: alterações ainda não commitadas, não enviadas e não
implantadas. O worktree já continha mudanças de implementação e outros
documentos feitas antes desta tarefa; elas foram preservadas e não estão sendo
declaradas como concluídas por este registro.
