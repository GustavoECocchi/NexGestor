# Sessão 2026-09-05

## Análise da execução do Sonnet e prompt para Opus 5

Pedido: analisar o resultado executado pelo Claude Code e substituir
docs/rascunho_prompt.md por instruções para revisar e corrigir possíveis erros.

Lidos AGENTS.md, CLAUDE.md, roadmap, prompt anterior, relatório da parte 6
de 2026-09-04.md, trechos pertinentes do PRD/contrato e código/testes atuais.
HEAD continua em 63f9645; as mudanças preexistentes foram preservadas.

### Evidências

Reproduções em memória/bancos temporários, sem editar código do produto:

- Grounding aceitou VALOR: 12.34 com support [7, 8), embora o número
  ocupe [7, 12): a validação permite sobreposição parcial.
- _cache_e_valido aceitou CTR 999 e data inválida; valor textual causou
  TypeError. BenchmarkEncontrado aceitou 999, fonte só com espaço,
  URL https:// sem host e timestamp vazio.
- Inicializar banco A e trocar DB_PATH para B sem reset de _iniciado
  produziu OperationalError: no such table: benchmarks_mercado.
- Com eventos asyncio e busca mockada, cancelar o chamador cancelou a
  tarefa compartilhada; retry iniciou a segunda busca. O double usado foi
  assíncrono; o prompt exige teste adicional do executor síncrono bloqueado.
- Executando store.ts transpilado em memória com localStorage simulado,
  enriquecer e depois marcarComoSalva com snapshot original apagou as
  referências. mesclarComServidor com payload original também as apagou.
  removeLive seguido de save tardio recriou a campanha; essa corrida
  envolve o save, não o callback isolado de benchmark.
- Leitura de App.tsx: enriquecimento não é enviado ao backend.
  App.benchmark.test.tsx mocka a camada API, inclusive a validação dos
  casos malformados; o teste de exclusão altera localStorage diretamente.

Encaminhados como investigação adicional, sem alegar reprodução completa:
cache legado, referências restauradas de payload opaco, expiração,
paridade entre quatro listas, motivos de fallback, status e logs.

### Validação

- Backend: python -m pytest -q test_benchmark_mercado.py
  test_regressao_20260904.py — 113 passaram, 1 warning de depreciação do
  SDK, em 1,77s. IA desativada, chave fictícia, DB_PATH vazio e fixtures
  com bases temporárias. No sandbox, TestClient parou no primeiro teste;
  tentativa limitada expirou em 30s. Repetição fora do sandbox pelo
  mecanismo de aprovação passou. Tentativas paradas foram interrompidas.
- Dashboard: npm test -- src/test/components/App.benchmark.test.tsx
  src/test/components/NewCampaignModal.test.ts src/test/lib/adapt.test.ts
  — 107 passaram, com warnings de updates React fora de act.
- As reproduções complementam as suítes verdes e evidenciam lacunas.
  Suítes completas, lint e build não foram repetidos nesta entrega
  documental; o prompt exige executá-los após as correções.

### Entrega

docs/rascunho_prompt.md substituído por prompt para Opus 5 revisar os sete
lotes, corrigir falhas confirmadas e acrescentar testes reais na fronteira
HTTP, com critérios de aceite e rastreabilidade. Preservadas as decisões:
benchmark informativo, só CTR elegível, feature desligada, compatibilidade
de nicho e análise não bloqueante. O texto não seleciona o modelo no Claude.

Status da tarefa documental: concluído e validado por revisão das evidências,
diff e checagem de whitespace. Falhas do produto não foram corrigidas nesta
tarefa: foram encaminhadas ao Opus. Roadmap sem mudança de fase por esta
preparação; sua reconciliação é exigida após a revisão/correção.

Nada commitado, enviado ou implantado. Sem Gemini real ou acesso a produção.

## (parte 2) Revisão independente do Opus 5 + correção dos achados

Revisão COM implementação: cada falha alegada na parte 1 foi reproduzida de
forma independente antes de qualquer correção, corrigida, e travada por
regressão que falha sem a correção.

### Achados confirmados por reprodução (todos corrigidos)

**P0 — perda de dados (frontend, `src/lib/store.ts`)**

1. `marcarComoSalva` escrevia o snapshot `vm` por cima do estado atual:
   `upsertLive` → `aplicarBenchmarkNaLive` → `marcarComoSalva` **apagava os
   benchmarks** (reproduzido). Corrigido para mesclar sobre o estado ATUAL,
   localizado por `clientId` (identidade estável) via `indiceAtual`.
2. A mesma função **recriava campanha já apagada** quando o save em voo
   voltava depois do delete (reproduzido). Agora é no-op quando a campanha
   não está mais na lista.
3. `mesclarComServidor` sobrescrevia o local com o payload do servidor
   (gravado antes do benchmark chegar), **apagando o enriquecimento**
   (reproduzido). Agora preserva `benchmarks` locais quando o servidor ainda
   não os tem; o servidor vence quando já tem.
4. O enriquecimento **nunca era enviado ao backend** — só existia neste
   navegador, e a primeira mesclagem o perdia. `App.tsx` passou a reenviar a
   versão enriquecida com o MESMO `client_id`. **Suspeita do prompt refutada
   com evidência**: reenviar com o mesmo `client_id` NÃO é
   "idempotente-ignorando-conteúdo" — `storage.salvar` faz
   `ON CONFLICT (dono, client_id) DO UPDATE SET payload = excluded.payload`,
   ou seja, atualiza o conteúdo (provado gravando `v1` e depois `v2`).
   `marcarFalhaPermanente` e `registrarAvisoTransitorio` também passaram a
   usar identidade estável (achavam por `id`, que muda na reancoragem).

**P0 — fonte incorreta (backend, `app/service/benchmark_service.py`)**

5. Cobertura **parcial** era aceita: `VALOR: 12.34` com support `[7, 8)` — 1
   byte de 5 — atribuía o número àquela fonte (reproduzido). Agora exige
   cobertura INTEGRAL do trecho (`_cobertura_integral`, aceita a união de
   supports adjacentes).
6. **Achado novo, não previsto no prompt**: os offsets eram tratados como
   caracteres do texto concatenado. O SDK documenta `Segment.start_index/
   end_index` como **bytes**, relativos a uma **Part** (`part_index`) — que o
   código ignorava. Com resposta multi-parte ou qualquer caractere multibyte,
   a atribuição olhava o trecho errado. Corrigido com
   `_span_em_bytes_da_parte` + filtro por `part_index`; valor que atravessa
   duas partes é rejeitado como ambíguo.

**P1 — custo duplicado (backend)**

7. `await tarefa` direto: cancelar UM interessado cancelava a tarefa
   COMPARTILHADA, e o retry abria uma **segunda busca paga** (reproduzido —
   2 chamadas). Corrigido com `asyncio.shield`. Testado com double
   assíncrono E com double SÍNCRONO bloqueado no executor (caminho real do
   SDK). Registrado no código que o single-flight é **por processo**: com
   `--workers N` o pior caso são N chamadas, não 1.

**P1 — validação (backend)**

8. `_cache_e_valido` aceitava CTR 999/-5/0, `capturado_em` inválido, fonte só
   com espaço e `https://` sem host; valor textual levantava `TypeError` em
   vez de virar miss (todos reproduzidos). Reescrito: nunca levanta, valida
   faixa/tipo (bool rejeitado)/fonte/URL com host/data ISO real.
9. `BenchmarkEncontrado` (response_model) aceitava exatamente os mesmos
   (reproduzido). Mesmas invariantes aplicadas; `extra="forbid"` nos dois
   modelos de resposta.
10. **Cache legado**: positivos gravados sob a regra de cobertura parcial e
    negativos vindos de falha transitória continuariam válidos por 14 dias.
    Adicionada coluna `regra_versao` (`REGRA_BENCHMARK_VERSAO = 1`); linhas
    de versão anterior viram miss. **Invalida só benchmarks** — campanhas e
    dados do usuário não são tocados.
11. Falha de SQLite ao ler o cache virava "sem fonte"; agora vira
    `BenchmarkIndisponivel` (503), nunca ausência de benchmark.

**P2 — troca de `DB_PATH` (backend)**

12. `_iniciado` era booleano global: trocar `DB_PATH` deixava o módulo
    achando que a base nova já tinha schema → `OperationalError: no such
    table` (reproduzido). Agora `_iniciado_para` guarda o caminho e a troca
    reinicializa. Regressão escrita SEM resetar o flag, de propósito — a
    lacuna existia justamente porque as fixtures o resetavam.

**P2 — motivos genéricos**

13. Fallback de custo usava o texto de "não existe benchmark", afirmando uma
    conclusão que a busca nunca tirou. Agora há `motivo_tipo`
    (`nao_elegivel` × `nao_encontrado`) e textos distintos: falta de contrato
    de moeda é limitação nossa, não ausência de fonte no mercado.

**P2 — validação de dados restaurados (frontend)**

14. `api.ts` validava só prefixo/finitude/string de data. Agora `0 < value
    <= 100`, URL com host analisável (`new URL`) e data realmente parseável.
15. `BenchmarkNote` renderizava `href`/`toFixed` direto sobre payload vindo
    do `localStorage`/da API de campanhas — caminhos que a validação da
    resposta nova nunca tocou. Agora revalida com a MESMA função
    (`referenciaDeMercadoValida`) antes de renderizar.

### Lacunas de teste fechadas

- **Integração HTTP real** (`App.benchmark.http.test.tsx`, 28 testes):
  `fetch` é o único mock; corpo cru trafega de verdade. Cobre 15 corpos
  malformados (item `null`, HTML de proxy, JSON inválido, `value` string/fora
  de faixa, URL sem host/`javascript:`, fonte em branco, data inválida,
  métrica não pedida/duplicada, discriminante ausente), 404/422/500/501/503,
  queda de rede, gating por `/status` (3 variantes), caminho feliz com link
  seguro + **prova de que o payload enriquecido sobe com um único
  `client_id`**, e exclusão pela **ação real da UI** (lixeira → confirmação),
  não mexendo no `localStorage` direto.
- **Corridas de persistência** (`store.corridas.test.ts`, 11 testes).
- **Fixtures do SDK**: `test_benchmark_mercado.py` trocou `MagicMock` por
  objetos REAIS do `google.genai` — foi o que expôs o bug de bytes/`part_index`
  (mock nenhum reproduz isso). Inclui multi-parte e acento.
- **Paridade entre linguagens**: as quatro fontes (schema Python, labels
  Python, `types.ts`, `niche.ts`) + o contrato, cada uma derivada do próprio
  arquivo — sem uma quinta lista literal.
- **Avisos de `act`**: 13 → **0**, corrigindo a coordenação dos testes (flush
  dentro de `act`), sem suprimir avisos.

### Refutado com evidência

- `client_id` **não** ignora conteúdo novo (ver item 4).
- Redação de chave nos logs funciona: com sentinela fictícia, nem o log nem a
  mensagem da exceção propagada expõem a chave.

### Validação executada

- Backend: `python -m pytest -q` → **1674/1674** (era 1626 antes desta
  rodada), 1 warning de depreciação do SDK. IA desligada por `conftest.py`,
  chave fictícia, bancos temporários. `TestClient` rodou sem travar.
- Dashboard: `npm test` → **513/513** (era 474), **0 avisos de act**;
  `tsc -b` limpo; `npm run lint` exit 0 (12 warnings pré-existentes, nenhum
  novo); `npm run build` limpo.
- OpenAPI: `BenchmarkResposta` como `response_model` (união discriminada),
  `motivo_tipo` com enum, `additionalProperties: false` na entrada e na
  resposta positiva.
- `git diff --check` limpo.
- Mutação (edição reversível controlada, sem `git checkout`): remover
  `_cobertura_integral` derruba o teste de cobertura parcial; remover
  `asyncio.shield` derruba os dois testes de cancelamento.

### Estado de entrega

Implementado e validado localmente. **Nada commitado, enviado ou
implantado** — HEAD continua em `63f9645`. Grounding real do Gemini, A2 do
nginx e ativação em produção permanecem **fora** desta execução e não foram
validados.

## (parte 3) Prompt para aumentar a legibilidade e escala do dashboard

Pedido: após feedback de um professor do ITA sobre dificuldade de leitura,
preparar um prompt para Claude Code aumentar fontes e a escala da interface.
O pedido substituiu a solicitação anterior de nova revisão do benchmark.

Inspeção: CLAUDE.md, AGENTS.md, roadmap, style.css, estilos inline de
NewCampaignModal/CampaignDetail, scripts do dashboard e comando rascunho.
O CSS mantém muitos tamanhos de 8,5–13,5 px herdados da extensão, apesar
da fonte Atkinson Hyperlegible Next. Controles de 26/32 px e dimensões de
cards/sidebar/modal também precisam acompanhar a ampliação. São evidências
de código; nenhuma avaliação visual em navegador foi realizada nesta tarefa.

docs/rascunho_prompt.md foi substituído por instruções executáveis com:

- corpo de 18 px, labels/controles/textos importantes de pelo menos 16 px
  e piso de 14 px reservado a metadados auxiliares, como metas de design;
- tokens em rem respeitando a configuração do navegador, revisão dos
  estilos inline, espaçamentos, controles e ícones proporcionalmente maiores;
- adaptação de grades/modais/sidebar sem diminuir a fonte para caber;
- cobertura de todas as telas, contraste nos dois temas e verificação de
  textos longos, teclado, zoom real e resoluções desktop/tablet/celular;
- capturas antes/depois, estilos computados e testes/lint/build para o
  executor; confirmação posterior de conforto pelo usuário/professor.

Estado desta tarefa documental: concluído e validado por revisão do prompt
contra o código e o pedido, git diff --check e checagem de whitespace.
Não foram executadas suítes do produto porque apenas prompt/histórico foram
alterados. O frontend ainda não recebeu a ampliação nesta tarefa.

Git no início desta rodada: worktree limpo, HEAD 81a1ca1 (o registro da
parte 2 descreve um momento anterior ao commit). Esta rodada alterou apenas
prompt e sessão; nenhuma mudança de fase no roadmap. Estas novas alterações
não foram commitadas, enviadas ou implantadas.

## (parte 4) Execução do prompt de legibilidade/escala (docs/rascunho_prompt.md)

Implementação da escala tipográfica pedida na parte 3, em
`frontend/nexgestor-dashboard`.

### O que mudou

- `src/style.css`: reescrito por completo. Adicionados 8 tokens
  semânticos em `:root` — `--text-caption` (14px, piso excepcional),
  `--text-label` (16px, piso de navegação/labels/campos/avisos),
  `--text-body` (18px, texto corrido/diagnóstico), `--text-section` (22px),
  `--text-title` (30px), `--text-metric` (32px), `--text-metric-lg` (36px,
  só o score de saúde) e `--control-h` (44px, altura mínima de
  controles/botões-ícone). `--text-metric-lg` e `--control-h` não estavam
  na lista de exemplo do rascunho — abertos porque um único
  `--text-metric` não sustenta a hierarquia entre o score principal e os
  demais números. `html{font-size:100%}` explícito. Praticamente todo
  `font-size` literal do arquivo foi substituído por um destes tokens;
  paddings/gaps/ícones/dimensões de componentes (sidebar 240→272px, cards,
  modal 640→760px desktop, score ring 74→96px, botões-ícone 32→44px)
  cresceram junto. `.card-status-label` e `.context-card .cv` usam valores
  fora dos tokens (1.5rem / 1.25rem) para preservar a hierarquia visual
  documentada no CSS (status do card é o elemento mais escaneável;
  métricas de contexto são deliberadamente apagadas). `--txt-3`/`--muted`
  foram trocados por `--txt-2` em todo texto agora classificado como
  "importante" (labels de métrica, avisos, texto de ajuda), reaproveitando
  a medição de contraste já documentada para `.ai-status` (7,72:1 escuro,
  ~5,9:1 claro — não foi refeita medição individual por elemento).
  Adicionada regra `@media(max-width:720px)` nova: sidebar vira barra
  superior que empilha (marca/nav/rodapé em linhas próprias, texto
  completo preservado, sem ocultar função nenhuma); `@media(max-width:480px)`
  novas para formulário, seletor de comparação e tabela de comparação
  (com rolagem horizontal local só na tabela).
- `src/components/CampaignDetail.tsx`: anel de score (SVG) de 74→96px,
  raio 32→40, `strokeWidth` 6→8 — geometria é atributo do SVG, não CSS,
  não acompanharia o token sozinha. Um `fontSize` inline (nota de
  confiança/cobertura) trocado por `var(--text-label)`.
- `src/components/NewCampaignModal.tsx`: 10 `fontSize`/tamanhos inline
  trocados pelos tokens (alertas de erro, pré-visualização de arquivo,
  rótulos MÉTRICAS/METAS). Ícone de "etapa concluída" 14→20px.
- `src/components/FieldHint.tsx`: `TIP_WIDTH` 220→280px (o tooltip usa
  `--text-body`, precisa de mais largura pra quebrar linha de forma
  legível).
- **Bug real encontrado e corrigido durante a validação em 390px**: os
  dois `style={{ gridColumn: "1/3" }}` de `NewCampaignModal.tsx` (campo
  "Nome da campanha" e "Aprendizado limitado", ambos span-2-colunas)
  assumiam grid de 2 colunas sempre. Com `.fld-grid` colapsando pra 1
  coluna em telas estreitas (`@media max-width:480px`), pedir a linha 3
  a um grid de 1 coluna (2 linhas) cria uma 2ª coluna IMPLÍCITA só pra
  caber o span — e os campos seguintes sem posicionamento explícito
  (Objetivo/Plataforma) voltavam a cair lado a lado nessa coluna
  fantasma, mesmo com `grid-template-columns:1fr` declarado. Corrigido
  trocando para `"1/-1"` (última linha do grid, seja qual for) — testado
  e confirmado tanto no formulário estreito (stack correto) quanto no
  desktop (span dos 2 columns preservado).

### Validação

- `pytest` não se aplica (mudança é só frontend). `npm test -- --run`:
  **513/513**, sem novos avisos de `act()`. `npx tsc -b`: limpo.
  `npm run build`: limpo. `npm run lint` (oxlint): mesmos avisos
  pré-existentes de antes desta tarefa (nenhum novo, nenhum erro).
  `git diff --check`: limpo.
- Visual, em navegador real (Chrome via automação, `npm run dev`, dados
  mock de `data/mock.ts`): DonoGate, Home (resumo/chips/fin-grid/cards),
  detalhe de campanha completo (score, Faixa, Funil+Ações, Diagnóstico,
  Ações prioritárias, Sugestões, Copiloto), modal de nova campanha
  (manual, tooltip de campo), Central de Ajuda, paleta de comandos
  (⌘K), comparação de campanhas — nos dois temas (claro/escuro).
  Resoluções: a **redimensão real da janela do Chrome não funcionou
  neste ambiente** (`resize_window` não altera `window.innerWidth`,
  preso em ~1910px independente do valor pedido — limitação do ambiente
  de automação, não da aplicação). Contornado com uma página local
  hospedando a SPA num `<iframe>` de largura fixa (390px e 768px, altura
  generosa para não precisar rolar dentro do iframe) — isso testa os
  mesmos breakpoints CSS de largura de viewport, mas não é o mesmo que
  uma janela real menor. **Zoom real do navegador (100%/200%) também não
  foi possível via automação** (o atalho de zoom do navegador é
  bloqueado pela própria ferramenta); usado `document.documentElement.
  style.zoom = "2"` como aproximação grosseira só pra checar ausência de
  corte/sobreposição óbvia a 2×, o que **não é equivalente a zoom real de
  navegador** (não afeta `window.innerWidth`/media queries do mesmo jeito).
  Ambas as limitações — janela redimensionável de verdade e zoom real —
  ficam como **validação visual pendente**, a fazer manualmente num
  navegador desktop comum.
- Estilo computado não foi extraído campo a campo (nenhum DevTools
  Computed Style aberto); a confirmação foi visual, por captura de tela,
  em pelo menos um exemplo de cada categoria tipográfica (caption/label/
  body/section/title/metric) nas telas listadas acima.
- Não foi possível testar teclado (foco visível, navegação) nem leitor
  de tela nesta rodada — fora do escopo coberto pelas ferramentas de
  automação disponíveis.

### Tamanhos principais, antes → depois (px, referência 16px/100%)

Corpo/diagnóstico 12→18; labels/navegação/avisos 9,5–13,5→16;
metadados/atalhos 8,5–10→14 (piso); títulos de card/seção 13,5–15,5→22;
títulos de página/modal 18–21→30; métricas secundárias (Faixa/Resumo)
15–24→32; score de saúde 20→36. Sidebar 240→272; modal desktop 640→760;
botões/campos principais →44 de altura mínima; botões-ícone 32×32→44×44
(exceção documentada: lixeira do card, 40×40, por espaço no canto do
card). Anel de score 74→96px.

### Estado de entrega

Implementado e validado localmente (suíte, build, lint, e validação
visual manual via automação de navegador, com as duas lacunas acima
registradas como pendentes). **Nada commitado, enviado ou implantado**
— HEAD continua em `81a1ca1`. Roadmap não muda de fase por esta tarefa
(item 10 já cobre o dashboard). O usuário/professor do ITA ainda precisa
experimentar a versão para confirmar se o novo tamanho atende ao
conforto de leitura esperado — isso não foi (e não poderia ser)
verificado nesta execução.
