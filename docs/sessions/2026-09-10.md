# Sessão 2026-09-10

## Retomada — conferência do checkpoint

- Pedido: voltar ao desenvolvimento. Contexto recuperado por leitura de
  CLAUDE.md, roadmap, índices e checkpoint de transferência de 09/09.
- Base observada: `fix/p5-validacao-consistencia-metricas`, HEAD `90d7724`.
  Status inicialmente sem alterações identificadas; diffs local e staged
  vazios. Git avisou falta de acesso a dois diretórios `.pytest_cache`.
- Conferência concluída por inspeção do histórico e dos arquivos atuais:
  snapshot P5 em `52fb515`, seguido do registro documental `90d7724`;
  exigência de campos obrigatórios e arredondamento Decimal local presentes.
  Isso confirma o ponto de retomada, não constitui revisão do produto.
- P5 tem validação local registrada em 09/09; revisão independente final
  continua pendente. Diagnóstico parcial apenas especificado. O rascunho
  preservado é anterior às últimas correções e não foi executado.
- Próxima ação documentada: revisão final P5, tratar eventuais achados e
  coordenar integração. Seleção da atividade/agente a alinhar com o usuário.
- Nenhum teste repetido, mudança de produto, consulta remota, commit, push
  ou deploy nesta conferência. Envio do snapshot é evidência de 09/09,
  não verificação remota atual. Roadmap sem mudança de fase.
- Registro e índice validados por revisão do diff e `git diff --check`.
  Alterações documentais desta retomada permanecem locais.

## Preparação — revisão final P5 pelo Claude

- Pedido: preparar material para Claude revisar o trabalho realizado;
  usuário confirmou o destino `docs/rascunho_prompt.md`.
  Substituído o prompt antigo de correções por revisão do delta final.
- Base reconferida: branch P5, HEAD `90d7724`, snapshot `52fb515` sobre
  `692a0f9`. Alterações documentais da retomada preservadas. Conferidos
  código atual de obrigatórios/Decimal e conclusão final de 09/09.
- Escopo: revisão independente dos 23 obrigatórios do CampaignVM,
  compatibilidade de gravação/leitura/sincronização e formatação Decimal
  de valores finitos grandes, com regressões e verificações focadas.
  Prompt distingue snapshot completo do delta final sem commit isolado.
- Estado: prompt concluído e validado documentalmente por conferência
  de referências, comandos e diff; `git diff --check` sem erros.
  Revisão do Claude ainda não executada. Nenhuma correção de produto,
  suíte, chamada paga ou comunicação externa executada nesta preparação.
- Próxima ação: usuário levar o rascunho ao Claude para revisão e registro
  do veredito. Sem integração de governança ou diagnóstico parcial.
  Roadmap sem mudança de fase; alterações locais, sem commit/push/deploy.

## Revisão independente final — P5 (Claude)

- Pedido: `docs/rascunho_prompt.md` — revisão independente do delta final de P5
  (23 obrigatórios do CampaignVM + arredondamento Decimal da mensagem), sem
  alterar produto, testes ou configuração.
- **Versão examinada:** branch `fix/p5-validacao-consistencia-metricas`,
  HEAD `90d7724`, índice vazio. Único diff local: documentação
  (`docs/rascunho_prompt.md`, `docs/sessions/README.md`, este arquivo).
  `git diff --stat -- backend frontend` vazio ao fim da revisão: nenhum arquivo
  de produto ou teste foi tocado.
- **Estado desta tarefa: concluído e validado.** Delta final revisado e
  reproduzido; dois achados abertos, nenhum deles introduzido pelo delta final.

### Validação executada nesta revisão

Comandos e resultados desta execução (não os de 09/09):

- `backend/backend-nexgestor-main`, com `PYTHONUTF8=1`, `GEMINI_ENABLED=false`,
  `BENCHMARK_ENABLED=false`:
  - `py -X utf8 -m pytest -q test_metric_consistency.py -p no:cacheprovider`
    → **245 passed** (6,33s).
  - `py -X utf8 -m pytest -q test_storage.py test_regressao_20260825.py
    test_regressao_20260904.py -p no:cacheprovider` → **141 passed**.
    Ampliação justificada: os achados tocam o contrato de gravação.
- `frontend/nexgestor-dashboard`:
  - `npm.cmd test -- src/test/components/CampaignVM.contrato.test.tsx`
    → **8 passed**.
  - `npx.cmd tsc -b` → sucesso (exit 0).
  - `npm.cmd test -- src/test/lib/sync.test.ts src/test/lib/api.test.ts`
    → **53 passed** (confere a classificação 422-permanente na sincronização).
- Reproduções próprias em `TestClient` + SQLite temporário fora do repositório,
  IA e benchmark desligados, sem dados reais, chamadas pagas ou edição de `.env`.
  Dois arquivos de teste temporários foram criados sob `src/test/` para dirigir
  o adapter e os componentes, e **removidos** ao fim (`git status` limpo).

Reproduções que confirmam o delta final:

- **Obrigatórios (23).** Lista derivada independentemente de `src/types.ts`
  (regex sobre a interface `CampaignVM`) bate exatamente com
  `_CHAVES_OBRIGATORIAS`; `_CHAVES_CONHECIDAS` bate com obrigatórios+opcionais
  (34). Remover cada um dos 23 de um VM real → **422 em 23/23, sem gravar**
  (contagem em `SELECT count(*)` antes/depois). Remover cada opcional presente
  na fixture → 200 em 6/6. Métricas `null` + `tiles`/`scenarios`/`actions`/
  `sugg`/`spark` como `[]` + só os 23 obrigatórios → 200.
- **Fluxos legítimos.** Dez respostas reais de `/api/v1/campaign/analyze`
  (completa, mínima, só gasto, sem métricas, zeros, sem ROAS, ROAS 0, fadiga,
  vídeo, amostra baixa) convertidas pelo `responseToVM` real: **10/10** passam
  no contrato, gravam e releem idênticas. Nenhum fluxo legítimo foi bloqueado.
- **Idempotência e legados.** Reenvio com o mesmo `client_id` → mesmo `serverId`,
  1 linha. Atualização por `id` → 1 linha, nome atualizado. Linha legada
  inserida direto no SQLite (sem obrigatórios) continua devolvida **intacta**
  pelo GET; reenviá-la dá 422 sem corromper a linha existente. O ramo 422 →
  `permanente: true` em `frontend/nexgestor-dashboard/src/lib/api.ts:520-534`
  separa corretamente compatibilidade de leitura de compatibilidade de escrita,
  e a campanha sai do laço de sync com aviso visível
  (`src/components/CampaignCard.tsx:183-186`).
- **Decimal / mensagem.** `1e25`, `1e26`, `1e30`, `1.5e300`, `1e308` e
  `sys.float_info.max` em R1 e R2 → **422 em todos**, nenhum 500, nenhuma
  `InvalidOperation`; idem para `hook_rate`, `hold_rate`, `ctr_all` e
  `lp_conversion_rate`. Brutos inteiros sem teto no schema (`10**30`, `10**60`,
  `10**400` no numerador e no denominador) → 422, sem 500 e sem travar
  (< 0,02s). Contexto Decimal global após todas as chamadas permanece
  `prec=28`, `rounding=ROUND_HALF_EVEN`.
- **`_arredonda` contra referência independente.** Comparado com ROUND_HALF_UP
  exato em `Fraction` em 11 casos de fronteira (transporte 9,99→10, 99,995→100,
  meio-caminho 0,5/1,5/2,5, negativos, `1E+30` com 2 casas, `1E-7` com 10):
  **0 divergências**. `_fmt_pct(100, 0) == "100"` — a remoção de zeros só atua
  na parte fracionária.
- **A regra não mudou.** `contradiz_os_brutos` comparado com uma referência
  exata em `Fraction` (tolerância = `max(10^-k/2, 0.005)`, `k` = casas escritas)
  em **4.013 casos** (13 fixos + 4.000 aleatórios, semente 20260910):
  **0 divergências**. O ajuste da mensagem não moveu a fronteira de R1/R2.

### Achados

**1. Média — `status: "PAUSED"` passa no contrato de gravação e derruba o
consumidor.** Mesma classe do achado 1 da revisão Codex (`tiles: [null]`):
um valor que a escrita aceita e a tela não renderiza.

- Onde: `backend/backend-nexgestor-main/app/service/campaign_payload.py:108`
  (`_STATUS_DE_CAMPANHA`, usado em `:393-398`) aceita cinco status;
  `frontend/nexgestor-dashboard/src/types.ts:173` define
  `UIStatus = "RED" | "YELLOW" | "GREEN" | "BLUE"` — quatro.
  Consumidores: `src/lib/status.ts:3-11` (`STATUS: Record<UIStatus, …>`),
  lido sem fallback em `src/components/CampaignCard.tsx:75-76`,
  `src/components/CampaignDetail.tsx:31` e `src/components/DetailSections.tsx:19`.
- Reprodução: `POST /api/v1/campaigns` com a fixture `campaignVMReal.json` e
  `status: "PAUSED"` → **HTTP 200, gravado**; o `GET` devolve `PAUSED`
  (`paused` minúsculo e `ROXO` dão 422, como esperado). Renderizando esse
  mesmo VM: `CampaignCard` e `CampaignDetail` lançam
  `TypeError: Cannot read properties of undefined (reading 'bg')`.
  `Summary` não quebra, mas a campanha não entra em nenhuma contagem por status
  enquanto `n = campaigns.length` a inclui.
- Esperado × observado: o contrato se declara "a forma fechada do CampaignVM
  que o dashboard monta" e existe para garantir que o que grava é renderizável
  (`campaign_payload.py:14-18`). Esperado: recusar `PAUSED` na escrita (ou o
  consumidor ter fallback). Observado: aceito e persistido.
- Impacto: **não alcançável pelo fluxo do próprio dashboard** — o adapter
  converte `PAUSED` em `YELLOW` (`src/lib/adapt.ts:147`) e o enum do backend
  marca `PAUSED` como "reservado — uso futuro" (`app/enum/campaign.py:15`).
  Alcançável por cliente que chame a API direto, que é exatamente o modelo de
  ameaça documentado no módulo. Efeito: a tela da campanha daquele dono quebra
  ao abrir a lista.
- **Escopo: dentro da P5, fora do delta final sob revisão.** O enum de status
  é anterior aos dois ajustes finais; não foi introduzido nem reaberto por eles.
- Correção recomendada (exige novo pedido): restringir `_STATUS_DE_CAMPANHA` a
  `UIStatus` — é o que casa com a premissa de forma fechada, e o adapter nunca
  emite `PAUSED`. Regressões: `POST` com `PAUSED` → 422 sem gravar, e uma
  guarda de deriva comparando `_STATUS_DE_CAMPANHA` com `UIStatus` em
  `types.ts`, nos moldes de `test_toda_chave_do_campaign_vm_e_conhecida`.
  Se `PAUSED` for mantido de propósito, a decisão pertence ao consumidor: dar
  fallback em `STATUS` e cobrir a renderização.

**2. Baixa — não existe guarda de deriva para `_CHAVES_OBRIGATORIAS`.**
A lista está correta hoje; o que falta é o que a mantém correta.

- Onde: `backend/backend-nexgestor-main/test_metric_consistency.py:721-737`
  (`test_toda_chave_do_campaign_vm_e_conhecida`) compara `types.ts` só com
  `_CHAVES_CONHECIDAS` — o regex `^  ([A-Za-z0-9_]+)\??:` descarta o `?` e
  perde a distinção obrigatório/opcional. A lista dos 23 em
  `test_metric_consistency.py:790-795` é literal e tem o mesmo ponto cego.
- Reprodução (simulada sobre uma cópia em memória de `types.ts`, sem editar o
  arquivo): acrescentar um campo **obrigatório** novo ao `CampaignVM` faz a
  guarda de conhecidas falhar e obriga a atualizar `_CHAVES_CONHECIDAS`; feito
  isso, a suíte volta ao verde com `_CHAVES_OBRIGATORIAS` dessincronizada
  (`falta ['niche']` no exercício). No sentido inverso, marcar `primaryAction`
  como opcional em `types.ts` mantém toda a suíte verde com o backend ainda
  exigindo o campo.
- Esperado × observado: esperado que a suíte acuse qualquer divergência entre o
  tipo e o contrato de escrita, como já faz para o conjunto de chaves.
  Observado: só o conjunto total é guardado.
- Impacto: campo novo obrigatório → a escrita volta a aceitar VM sem um campo
  que o consumidor lê sem fallback, reabrindo o contorno do achado 1 do Codex.
  Obrigatório que vire opcional → VMs legítimos passam a receber 422 e o
  dashboard os marca como permanentemente não sincronizáveis.
- Correção recomendada (exige novo pedido): estender a guarda existente para
  derivar também o subconjunto sem `?` e comparar com `_CHAVES_OBRIGATORIAS`.

### Observações que não são achados

- `_MAX_CASAS_NA_MENSAGEM = 10` (`app/service/metric_consistency.py:111`):
  um valor declarado com mais de 10 casas aparece arredondado na frase que pede
  para conferir aquele campo. **Não esconde a diferença rejeitada** — a
  tolerância de R1 é de no mínimo 0,005pp, visível já na 3ª casa (conferido).
  Limite anterior ao delta e deliberado; registrado como limite, não defeito.
- O ajuste "100 com zero casas não vira 1" está correto, mas hoje é defensivo:
  todos os caminhos de produção chamam `_fmt_pct` com `casas >= 2`
  (`_casas_para_nao_sumir` nunca devolve menos que 2), então `casas=0` só é
  exercitado pela chamada direta em `test_metric_consistency.py:266`. Não há
  500 nem número trocado alcançável por HTTP por esse caminho.
- `test_o_valor_declarado_nunca_e_trocado_por_outro`
  (`test_metric_consistency.py:292-302`) usa o próprio `_fmt_pct` como
  referência. Os demais testes da classe usam literais (`"1,234%"`, `"1,228%"`,
  `"0,004%"`, `"33,33%"`, `"1,1%"`), então a exigência de referência
  independente está atendida no conjunto.
- `docs/CONTRATO_API_FRONTEND.md:471-525` descreve corretamente os 23
  obrigatórios, os 11 opcionais, a forma dos blocos e a política de legados.
  Único ponto a acompanhar junto do achado 1: a linha de `status` documenta
  `PAUSED` como aceito.

### Veredito

- **Delta final (23 obrigatórios + Decimal): aprovado.** Faz o que a conclusão
  de 09/09 afirma, pelas reproduções acima, sem quebrar fluxo legítimo, leitura
  de legados, idempotência ou a fronteira de R1/R2. Os dois pontos que o Codex
  deixou em aberto estão fechados.
- **Impedimento técnico à integração de P5:** nenhum vindo do delta final.
  O achado 1 é uma brecha remanescente da própria P5, da mesma classe que a P5
  se propõe a fechar, e é barata de corrigir — recomendo fechá-la antes da
  integração; a decisão é do usuário. O achado 2 é guarda de regressão, não
  altera comportamento atual.
- **Aceites humanos continuam separados e não foram avaliados aqui:** leitura
  por leigo, leitor de tela, viewport real e confirmação externa do limiar de
  ThruPlay. Testes verdes não os substituem.

### Limites desta revisão

- Revisão restrita ao delta final e aos caminhos que ele toca. Não houve
  auditoria geral do repositório, navegador, chamada paga, consulta a produção
  nem execução da suíte completa do backend ou do dashboard (build/lint não
  executados: nenhuma configuração de frontend foi alterada).
- Vale para HEAD `90d7724` com o worktree acima. Se o código mudar, os trechos
  afetados precisam ser reavaliados.
- Nenhuma correção de produto aplicada — a tarefa era revisão. Os dois achados
  exigem pedido explícito para virarem correção.

### Continuidade — revisão final P5

```text
Continuidade — revisão independente final de P5
Objetivo/referência: docs/rascunho_prompt.md (revisão do delta final de P5)
Base observada: fix/p5-validacao-consistencia-metricas, HEAD 90d7724,
  índice vazio, diff local só documental
Estado: concluído e validado
Validado: delta final reproduzido (23 obrigatórios, Decimal, legados,
  idempotência, adapter); 245 + 141 passed no backend, 8 + 53 passed e
  tsc -b no dashboard; R1 sem divergência em 4.013 casos contra Fraction
Incompleto/não validado: aceites humanos (leigo, leitor de tela, viewport,
  fonte do limiar de ThruPlay); suítes completas e build não repetidos
Decisões/restrições: revisão somente leitura; correções exigem novo pedido
Edição/revisão: revisão do Claude concluída; nenhum arquivo de produto tocado
Próxima ação: usuário decide se corrige o achado 1 (status PAUSED) e o
  achado 2 (guarda de _CHAVES_OBRIGATORIAS) antes de integrar P5
Entrega: não commitado, não enviado, não implantado
```

## Correção — achados 1 e 2 da revisão independente final (Claude)

- Pedido explícito do usuário: corrigir os dois achados registrados na seção
  "Revisão independente final — P5 (Claude)" acima. Base: mesmo branch/HEAD
  `90d7724`, diff local só documental antes desta correção.
- **Estado desta tarefa: concluído e validado.**

### Achado 1 — `status: "PAUSED"` aceito na gravação

- Correção: `_STATUS_DE_CAMPANHA` em
  `backend/backend-nexgestor-main/app/service/campaign_payload.py:108`
  restrita aos quatro valores de `UIStatus` (`GREEN`/`YELLOW`/`RED`/`BLUE`),
  removendo `PAUSED`. Comentário atualizado explicando por que o enum do
  backend (que reserva `PAUSED` para uso futuro) não é a referência certa
  aqui — o adapter nunca emite esse valor e o consumidor não sabe desenhá-lo.
- `docs/CONTRATO_API_FRONTEND.md:484-488`: a lista de valores aceitos de
  `status` na gravação não cita mais `PAUSED`, com a mesma justificativa.
- Regressões novas em `test_metric_consistency.py`
  (classe `TestContratoDeGravacao`):
  - `test_status_paused_e_recusado_na_gravacao`: `POST` com `status: "PAUSED"`
    → 422, nada gravado.
  - `test_status_de_campanha_e_exatamente_o_ui_status`: guarda de deriva —
    deriva `UIStatus` de `types.ts` por regex e compara com
    `_STATUS_DE_CAMPANHA`, nos moldes de `test_toda_chave_do_campaign_vm_e_conhecida`.
    Acusa se um dos dois lados mudar sem o outro acompanhar.

### Achado 2 — sem guarda de deriva para `_CHAVES_OBRIGATORIAS`

- Correção: `test_toda_chave_do_campaign_vm_e_conhecida` (mesmo arquivo)
  estendido — além do conjunto total (`_CHAVES_CONHECIDAS`), agora deriva o
  subconjunto SEM `?` de `CampaignVM` em `types.ts` e compara com
  `_CHAVES_OBRIGATORIAS`. Cobre os dois sentidos reproduzidos na revisão:
  campo obrigatório novo esquecido na lista do backend, e obrigatório que
  vira opcional no tipo sem a exigência acompanhar.
- Nenhuma mudança de comportamento: `_CHAVES_OBRIGATORIAS` já batia com
  `types.ts` antes da correção — só passou a ser guardado por teste.

### Validação executada

- `backend/backend-nexgestor-main`, `PYTHONUTF8=1`, `GEMINI_ENABLED=false`,
  `BENCHMARK_ENABLED=false`:
  - `py -X utf8 -m pytest -q test_metric_consistency.py -p no:cacheprovider`
    → **247 passed** (245 + 2 novos: os dois testes de status).
  - `py -X utf8 -m pytest -q -p no:cacheprovider` (suíte completa) →
    **1924 passed**, 1 aviso de depreciação do SDK google-genai (mesmo aviso
    de 09/09, não relacionado a esta mudança).
- `frontend/nexgestor-dashboard`:
  - `npm.cmd test -- src/test/components/CampaignVM.contrato.test.tsx`
    → **8 passed** (fixture e adapter continuam corretos; VM válido nunca
    teve `status: PAUSED`, então não é afetado).
  - `npx.cmd tsc -b` → sucesso (exit 0).
- `git diff --check`: limpo (avisos LF/CRLF de sempre).

### Arquivos alterados nesta correção

- `backend/backend-nexgestor-main/app/service/campaign_payload.py`
- `backend/backend-nexgestor-main/test_metric_consistency.py`
- `docs/CONTRATO_API_FRONTEND.md`
- Este registro.

### Limites

- Não repeti suíte de storage/regressão isoladamente (já coberta pela suíte
  completa). Não toquei `app/enum/campaign.py` nem `app/service/prompts.py` —
  `PAUSED` ali pertence a `final_status` do motor de análise, campo diferente
  do `status` de gravação do CampaignVM; fora do escopo dos dois achados.
- Aceites humanos (leigo, leitor de tela, viewport real) continuam separados
  e não avaliados aqui.

### Entrega

Implementado e validado localmente. **Não commitado, não enviado, não
implantado.** Roadmap sem mudança de fase (P5 não tem item próprio nele).

## Revisão Codex — duas correções finais do Sonnet

- Pedido: usuário informou que Sonnet concluiu os dois ajustes, retomando
  a revisão focada combinada. Revisão de produto somente leitura;
  este registro é a única alteração feita pelo Codex nesta etapa.
- Base: `fix/p5-validacao-consistencia-metricas`, HEAD `90d7724`, índice
  vazio. Delta revisado: `campaign_payload.py` (+10/-4),
  `test_metric_consistency.py` (+46/-1) e contrato da API (+4/-1).
  Rascunho, índice de sessões e registros preexistentes preservados.
- **Veredito: duas correções aprovadas, nenhum novo achado neste delta.**
  A revisão anterior do Claude aprovou obrigatórios/Decimal; esta revisão
  fecha os dois ajustes subsequentes. Não foi refeita auditoria geral.
- `PAUSED`: contrato de escrita agora aceita exatamente os quatro valores
  de `UIStatus`. Conferidos tipo, mapa de apresentação, adapter (converte
  PAUSED em YELLOW), ordem de validação antes de `storage.salvar` e contrato
  documental. Regressão HTTP retorna 422 com indicação de status e verifica
  listagem vazia; guarda de paridade de status executada sem skip.
- Obrigatórios: teste extrai separadamente os campos sem `?` da interface
  CampaignVM e compara por igualdade com `_CHAVES_OBRIGATORIAS`, além da
  comparação do conjunto total. Assim detecta divergência de obrigatoriedade
  mesmo quando a lista de chaves conhecidas já foi atualizada. Tipo atual e
  regex conferidos; o comportamento de produto não mudou neste segundo ajuste.
- Validação executada nesta revisão:
  - Backend, `PYTHONUTF8=1`, `GEMINI_ENABLED=false`, `BENCHMARK_ENABLED=false`,
    `py -m pytest -q test_metric_consistency.py -p no:cacheprovider`:
    **247 passed**, sem skips. Persistência das regressões em bancos temporários.
  - Dashboard, `npm.cmd test -- src/test/components/CampaignVM.contrato.test.tsx`:
    **8 passed**; fixture produzida pelo adapter e consumidor preservados.
  - Primeiras tentativas impedidas pelo sandbox (execução do Python e leitura
    da configuração Vitest). Repetições autorizadas fora dele passaram;
    esses bloqueios ambientais não foram classificados como falha do produto.
  - Diff e índice reconferidos após os testes; `git diff --check` sem erros,
    apenas avisos de conversão LF/CRLF.
- Limites: suíte completa 1924 e TypeScript são evidência da execução do
  Sonnet acima, não novas execuções desta revisão. Não repetidos suítes
  completas, tsc/lint/build ou navegador: delta restrito ao enum de escrita
  e guardas de teste, sem alteração frontend/configuração. Não executadas
  chamadas pagas, consulta a produção ou aceite com leigo/acessibilidade.
- Estado: revisão concluída e validada; nenhum impedimento técnico identificado
  neste delta para preparar a entrega Git de P5. Aceites humanos permanecem
  separados. Próxima ação: commit/PR e integração conforme coordenação do
  usuário; governança e diagnóstico parcial continuam frentes separadas.
- Entrega: correções e registro locais, **não commitados, não enviados,
  não implantados**. Snapshot anterior continua distinto deste delta.
  Roadmap mantido, pois P5 não tem item próprio com mudança de fase.

## Entrega Git — P5 revisado

- Pedido: usuário autorizou o próximo passo proposto, commitar as correções
  e preparar o PR de P5. Escopo desta etapa: commit, push da branch e PR
  para main; integração/merge e deploy não executados por este pedido.
- Base remota consultada: main em `692a0f9`, branch P5 em `90d7724`;
  nenhuma divergência em relação às referências locais. Consulta GitHub
  confirmou ausência de PR anterior desta branch. Rede consultada com
  permissão após bloqueio no sandbox.
- Conteúdo: duas correções revisadas, contrato, prompt de revisão e evidências
  de 10/09. O PR também inclui o snapshot `52fb515`: implementação P5,
  testes e documentação de continuidade/organização já enviados em 09/09.
  Mini-PRD de diagnóstico parcial é especificação, não implementação.
- Validação reutilizada sobre o mesmo código: revisão Codex acima (247 + 8
  testes); execução completa do Sonnet registrada nesta sessão (1924 e tsc).
  Diff conferido sem alterações adicionais de produto e sem erros de whitespace.
  Nenhum motivo novo para repetir suítes neste passo de entrega.
- Estado: **entrega Git concluída e validada**. Commit `b7a80e2` criado
  com os seis arquivos conferidos e enviado à branch P5. PR
  [#6](https://github.com/GustavoECocchi/NexGestor/pull/6) publicado para
  main, aberto e fora de rascunho; descrição inclui compatibilidade,
  validações datadas e limites. Consulta após criação confirmou head
  `b7a80e2`, `MERGEABLE`/`CLEAN` e nenhum check de CI listado (isso não
  representa CI aprovado). Worktree sem alterações identificadas antes
  deste registro de confirmação.
- Entrega: correções **commitadas e enviadas**, PR publicado;
  **não integrado e não implantado**. Esta confirmação segue em commit
  documental posterior na mesma branch. Próxima ação: integração do PR #6
  conforme coordenação do usuário, seguida da governança em etapa separada.
  Roadmap sem mudança de fase; aceites humanos continuam pendentes.

## Preparação — prompt de integração do PR #6 pelo Claude

- Pedido: criar prompt para Claude executar o próximo passo, integração
  de P5. `docs/rascunho_prompt.md` substituído pelo roteiro de conferência,
  merge do PR #6, atualização segura da main e publicação do registro.
- Base local conferida: branch P5, HEAD `bbf3e06`, worktree inicialmente
  sem alterações identificadas, diffs local/staged vazios. Última consulta
  remota da etapa anterior confirmou PR aberto com esse head; estado de
  merge após o push estava UNKNOWN, explicitado no prompt para nova consulta.
- Incluídas referências às revisões aprovadas e validações datadas,
  conferência de head/base e proteções, preservação do trabalho local,
  tratamento de PR já integrado e distinção entre ausência de CI e aprovação.
  Prompt e este registro devem ser conferidos e enviados como documentação
  antes da verificação final da versão a integrar.
- Estado: prompt concluído e validado documentalmente por revisão do texto,
  referências e `git diff --check`. Nenhuma mudança de produto, suíte,
  consulta remota adicional, merge, commit ou push nesta preparação.
- Próxima ação: usuário executar o rascunho no Claude. Integração do PR #6
  autorizada pelo roteiro solicitado; governança e deploy permanecem fora
  desta tarefa. Alterações do prompt/registro locais, não enviadas.
  Roadmap sem mudança de fase.

## Integração — PR #6 mergeado na main (Claude)

- Pedido: executar `docs/rascunho_prompt.md` (integração do PR #6). Escopo
  autorizado: conferir versão, concluir registros documentais, commitar/
  enviar ajustes documentais e mergear o PR #6 quando as verificações
  estivessem satisfeitas.
- **Estado desta tarefa: concluído e validado. PR #6 integrado à main.**

### 1 — Commit documental e conferência final do PR

- Base local ao iniciar: branch `fix/p5-validacao-consistencia-metricas`,
  HEAD `bbf3e06`, diffs locais só em `docs/rascunho_prompt.md` (o próprio
  roteiro recebido) e `docs/sessions/2026-09-10.md` (seção de preparação do
  prompt). `git add` explícito nesses dois arquivos — sem `add` indiscriminado.
- Commit `0a6c51c` ("docs: registra preparacao do prompt de integracao do
  PR 6") e push para `origin/fix/p5-validacao-consistencia-metricas`.
- PR #6 reconsultado no GitHub após o push: `headRefOid` bate exatamente com
  `0a6c51c21c5d2390a834ea2493b7da6c75598bd5`; `baseRefOid` `692a0f9` (main
  antes do merge); `mergeStateStatus` `UNKNOWN` logo após o push, `CLEAN`/
  `MERGEABLE` numa segunda consulta ~10s depois — comportamento já previsto
  no prompt, não é achado. `state: OPEN`, `isDraft: false`,
  `statusCheckRollup: []` (repositório sem CI configurado; branch `main`
  sem proteção — `404 Branch not protected`). `reviews: []` (sem review
  formal do GitHub; a revisão de conteúdo é a registrada nesta sessão e em
  09/09, pelo Claude e pelo Codex).
- Escopo do PR conferido via `gh pr diff 6 --name-only`: backend de P5
  (`campaign_payload.py`, `metric_consistency.py`, rotas, `service.py`),
  testes correspondentes, `NewCampaignModal`/`api.ts`/fixture do dashboard,
  `docs/CONTRATO_API_FRONTEND.md`, `docs/PRD.md`, reorganização de
  `docs/README.md`/`docs/historico/`/`docs/prds/` e as sessões de 08–10/09.
  Bate com o descrito na "Entrega Git — P5 revisado": snapshot de
  implementação P5 (09/09) + as duas correções (10/09) + organização
  documental + mini-PRD de diagnóstico parcial (especificação, sem
  implementação de produto). Nenhum arquivo de governança (PR #5) presente.

### 2 — Merge

- Método: merge commit (`gh pr merge 6 --merge --match-head-commit
  0a6c51c21c5d2390a834ea2493b7da6c75598bd5`), sem `--delete-branch` e sem
  `--admin`. `allow_merge_commit: true` no repositório.
- **Bloqueio de permissão:** a primeira tentativa foi recusada pelo
  classificador do modo automático (ação irreversível/externa). Registrado
  ao usuário sem tentar contornar por outra via (ex.: API de merge direta);
  usuário autorizou explicitamente via pergunta direta. Head reconferido
  como inalterado antes de repetir a chamada — só então o merge foi executado.
- Resultado: PR #6 `state: MERGED`, `mergedAt: 2026-09-10T15:50:25Z`,
  `mergeCommit.oid: 5fec3a8cdca4aa725fcef197c27f11b740ae1eab`.
- `git fetch origin main`: `origin/main` avançou `692a0f9..5fec3a8`
  (fast-forward simples, commit de merge do GitHub no topo). Confirmado por
  `git merge-base --is-ancestor` nos dois sentidos: `5fec3a8` contém o commit
  de merge, e `692a0f9` é ancestral dele — sem force-push nem reescrita.

### 3 — Main local e branch P5

- `main` local estava em `692a0f9` (desatualizada, sem checkout ativo — o
  worktree seguiu na branch P5 o tempo todo). Fast-forward seguro confirmado
  antes de aplicar (`692a0f9` ancestral de `origin/main`), então
  `git fetch origin main:main` atualizou a ref local para `5fec3a8` sem
  trocar de branch nem tocar o worktree.
- Branch `fix/p5-validacao-consistencia-metricas` preservada local e
  remotamente (`delete_branch_on_merge: false`, sem `-d` no merge);
  `git ls-remote` confirma a remota ainda existe em `0a6c51c`.
- Worktree permaneceu limpo do início ao fim desta etapa
  (`git status`: "nothing to commit, working tree clean").
- PR #5 (governança) conferido intacto: `state: OPEN`, não tocado nesta
  integração.

### Validação

- Nenhuma suíte repetida nesta etapa: nenhuma mudança de produto entre o
  head revisado (`bbf3e06`) e o head integrado (`0a6c51c`) — só o commit
  documental do prompt de integração. Evidência de produto reutilizada,
  com autoria/data: revisão do Claude e correção do Sonnet em 10/09
  (backend completo 1924 passed, `CampaignVM.contrato.test.tsx` 8 passed,
  `tsc -b` aprovado) e revisão focada do Codex sobre as duas correções
  (backend focal 247 passed, consumidor 8 passed).
- `git diff --check` limpo antes do commit documental (avisos LF/CRLF de
  sempre). Conferência de estado remoto via `gh pr view`/`gh api` em cada
  etapa, sem chamada paga, dado real ou alteração de `.env`/produção.

### Limites

- Integração não é deploy: nada foi implantado em produção; o dashboard
  continua rodando só local. Aceites com leigo, leitor de tela e viewport
  real continuam pendentes e fora do escopo desta tarefa.
- Benchmark/Gemini não ativados, diagnóstico parcial/P6/P7 e governança
  (PR #5) não implementados nem tocados nesta etapa.
- Roadmap sem mudança de fase: P5 (consistência de métricas) não tem item
  próprio nele — não confundir com a fase-5 de vocabulário do roadmap.

### Entrega

- **PR #6 integrado à `main`** via merge commit `5fec3a8`, `mergedAt`
  2026-09-10T15:50:25Z. Commit documental `0a6c51c` commitado e enviado
  antes do merge. `main` local e remota (`origin/main`) confirmadas em
  `5fec3a8`. Branch `fix/p5-validacao-consistencia-metricas` preservada
  local e remotamente, não apagada.
- Próxima ação: coordenação do usuário sobre a próxima frente (governança
  via PR #5, diagnóstico parcial, ou outra). Nenhuma decidida ou iniciada
  nesta tarefa.

## Preparação — integração da governança pelo Claude

- Pedido: criar prompt para revisar e integrar PR #5 após P5 e recomendar
  Sonnet ou Opus. Destino: `docs/rascunho_prompt.md`, substituindo o roteiro
  de integração do PR #6 já executado.
- Base local conferida: main `b0b3818`, contendo merge P5 `5fec3a8` e
  registro final; branch docs/governanca-minima `c433626`. Worktree inicialmente
  sem alterações identificadas. Diff desde a base comum: nove arquivos
  documentais, com duas seções novas em CLAUDE.md. Sem nova consulta remota;
  o executor deve atualizar referências e consultar o PR antes de integrar.
- Prompt delimita revisão G01–G09, fonte única de política, preservação
  dos históricos/índices após P5, reconciliação por seção, validação documental,
  commit/push, merge com conferência do head e registro publicado. Dá atenção
  às sobreposições em docs/README.md e sessão de 08/09; não autoriza produto,
  governança alternativa, retomada de auditorias antigas ou deploy.
- Recomendação: Sonnet para esta integração documental de escopo definido;
  Opus se surgir ambiguidade substancial de política. Juízo de adequação à
  tarefa, coerente com a orientação oficial (Sonnet para trabalho cotidiano,
  Opus para raciocínio complexo):
  https://code.claude.com/docs/en/model-config#model-aliases
- Estado: preparação concluída e validada por confronto com Git, política,
  especificação e sessões, revisão do texto e `git diff --check`.
  Nenhuma suíte de produto, commit/push ou merge nesta preparação.
  Prompt e registro permanecem locais para transporte documental pelo Claude.
- Próxima ação: executar o rascunho no Claude; conferir, reconciliar e
  integrar apenas PR #5. Roadmap sem mudança de fase.

## Integração — PR #5 mergeado na main (Claude)

- Pedido: executar `docs/rascunho_prompt.md` (revisão e integração do PR #5
  de governança mínima sobre a main pós-P5). Escopo autorizado: reconciliar
  o PR com a main atual, validar que o delta continua exclusivamente
  documental, registrar e mesclar quando pronto — sem produto, deploy,
  retomada de P5/P6/P7/diagnóstico parcial ou do PR #4.
- **Estado desta tarefa: concluído e validado. PR #5 integrado à main.**

### 1 — Preparação sem perder trabalho

- Base ao iniciar: `main` local/remota em `b0b3818` (registro de integração
  do PR #6); branch `docs/governanca-minima` em `c433626`, PR #5 `OPEN`,
  base `692a0f9` (main anterior ao P5). Worktree principal com alterações
  locais preexistentes só em `docs/rascunho_prompt.md` (este roteiro) e
  `docs/sessions/2026-09-10.md` (seção de preparação acima) — preservadas
  intactas durante toda a tarefa, sem `git add` amplo nem stash.
- Trabalho de reconciliação feito em worktree isolado
  (`git worktree add ../nexgestor-worktrees/governanca-minima
  docs/governanca-minima`), sem tocar a main nem o worktree principal;
  removido ao final (`git worktree remove`), sem apagar a branch.

### 2 — Revisão e reconciliação do PR sobre a main atual

- `git merge main` dentro do worktree da branch de governança produziu dois
  conflitos, exatamente os sinalizados no prompt:
  - `docs/README.md`: a main havia reorganizado o índice pelo P5 (nova seção
    "Onde cada documento fica", links convertidos, catálogo histórico).
    Resolvido preservando a reorganização da main e reinserindo, no mesmo
    formato de link, o parágrafo que classifica
    `prds/nexgestor-governance-docs/` como proposta auditada (mesmo status
    das demais em "Propostas e prompts").
  - `docs/sessions/2026-09-08.md` (conflito add/add): a versão da main já
    continha integralmente o conteúdo da branch de governança mais as
    seções posteriores de implementação do P5 (confirmado por diff das duas
    versões — divergência de uma única frase de fechamento). Adotada a
    versão da main como base, restaurando apenas essa frase
    ("Roadmap sem mudança de fase; registro local fora do stage.") que só
    existia na branch de governança.
- Commit de merge `0a9d425` na branch `docs/governanca-minima`, enviado a
  `origin`. `CLAUDE.md` não teve conflito (sem alteração da main desde a
  base comum) e manteve as duas seções novas ("Achados, bloqueios e
  encerramento" e "Mudanças analíticas, contratos e dados") sem duplicar
  estados ou papéis existentes.
- Pontos de aceite do prompt conferidos: pacote 01–04 com avisos de proposta
  histórica e link relativo correto para `CLAUDE.md`
  (`../../../CLAUDE.md`, verificado por `test -f`); README do pacote aponta
  `CLAUDE.md` como fonte normativa e `docs/sessions/2026-09-08.md` como
  relatório real da auditoria; AGENTS.md inalterado, já aponta para
  `CLAUDE.md`, sem necessidade de ajuste.
- Os 8 exemplos de aceite da tabela de validação de
  `05-pr-governanca-minima.md` foram exercitados manualmente contra o texto
  final de `CLAUDE.md` (teste passa vs. requisito, achado sem dependência,
  bug externo que invalida a entrega, falha ambiental, revisor que só
  prescreve, auditoria técnica sem calibração empírica, edição concorrente,
  mudança de contrato/dados) — todos consistentes, sem contradição nem nova
  exigência de confirmação para ações já autorizadas.

### 3 — Validação da versão a integrar

- `git diff --stat main HEAD` na branch de governança, pós-merge: exatamente
  os 9 arquivos documentais esperados (`CLAUDE.md`, `docs/README.md`, os 6
  arquivos do pacote e 1 linha em `docs/sessions/2026-09-08.md`), 1237
  inserções, 0 remoções — delta continua exclusivamente documental, sem
  tocar backend, frontend, testes ou configuração do P5.
- `git diff --check` limpo (só avisos de LF/CRLF do Windows) antes e depois
  do merge de reconciliação. Links relativos novos/alterados conferidos
  manualmente (`../../../CLAUDE.md`, `../../sessions/2026-09-08.md`,
  `prds/nexgestor-governance-docs/README.md`).
- Nenhuma suíte de produto executada — mudança confirmada como exclusivamente
  documental; evidência de produto do P5 reutilizada dos registros de 09–10/09.
- Descrição do PR #5 atualizada (`gh pr edit 5 --body-file`) para refletir o
  conteúdo final, a reconciliação com a main pós-P5 e a validação acima.

### 4 — Merge

- Reconferido antes do merge: `state OPEN`, `headRefOid 0a9d425` (igual ao
  head local, sem mudança concorrente), `mergeable MERGEABLE`,
  `mergeStateStatus CLEAN`, `statusCheckRollup []` (repositório sem CI
  configurado), `branches/main/protection` → 404 "Branch not protected".
- **Bloqueio de permissão:** a primeira tentativa (`gh pr merge 5 --merge
  --match-head-commit 0a9d425...`) foi recusada pelo classificador do modo
  automático (ação irreversível/externa), mesmo padrão do PR #6. Registrado
  ao usuário sem tentar contornar por outra via; usuário autorizou
  explicitamente via pergunta direta. Head reconferido como inalterado
  antes de repetir a chamada — só então o merge foi executado.
- Resultado: PR #5 `state: MERGED`, `mergedAt: 2026-09-10T16:13:49Z`,
  `mergeCommit.oid: 19ce6b2c733ecdd0d92b067e7d0a1597ee1c3eef`. Sem
  `--admin`, sem `--delete-branch`.
- `git fetch origin main`: `origin/main` avançou `b0b3818..19ce6b2`.
  Confirmado por `git merge-base --is-ancestor` que `b0b3818` e `0a9d425`
  (commit de merge revisado na branch de governança) são ambos ancestrais
  do novo `origin/main` — fast-forward simples, sem force-push nem
  reescrita de histórico.

### 5 — Main local e branch de governança

- `main` local estava em `b0b3818`, com o worktree principal na própria
  `main` durante toda a etapa. Fast-forward seguro confirmado
  (`b0b3818` ancestral do novo `origin/main`), aplicado com
  `git merge --ff-only origin/main` — atualizou para `19ce6b2` sem tocar as
  alterações locais preexistentes em `docs/rascunho_prompt.md` e nesta
  sessão (arquivos fora do diff da PR #5, preservados intactos).
- Branch `docs/governanca-minima` preservada local e remotamente
  (`delete_branch_on_merge: false`, sem `-d` no merge); `git ls-remote`
  confirma a remota ainda existe em `0a9d425`.
- Worktree isolado de trabalho removido ao final (`git worktree remove`),
  sem apagar a branch.

### Limites

- Integração não é deploy: nada foi implantado em produção.
- Não foram retomados P5/P6/P7, diagnóstico parcial, benchmark/IA, auditoria
  geral nem o PR #4 fechado.
- Roadmap sem mudança de fase: governança é melhoria de processo registrada
  apenas nesta sessão, como já era o padrão para este pacote.

### Entrega

- **Implementado e validado**: sim — reconciliação, revisão dos 8 exemplos
  de aceite e conferência de links, localmente e antes do merge.
- **Commitado e enviado**: sim — commit de merge `0a9d425` em
  `docs/governanca-minima`, enviado a `origin` antes do merge do PR.
- **Integrado**: sim — PR #5 `MERGED` via commit `19ce6b2`, `origin/main`
  e `main` local confirmados em `19ce6b2`.
- **Implantado (deploy)**: não — mudança documental, sem deploy nesta
  entrega nem em outra frente.
- Próxima ação: coordenação do usuário sobre a próxima frente (P5/P6/P7 de
  precisão e confiabilidade, diagnóstico parcial, ou outra). Nenhuma
  decidida ou iniciada nesta tarefa.
